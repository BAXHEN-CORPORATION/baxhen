import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Baxhen — Gamified Sales Experiences",
  description:
    "We build interactive sales experiences — simulated calls, WhatsApp threads, SMS narratives — that guide your prospect to a yes, without feeling like a pitch.",
  icons: {
    icon: "/favicon-32.png",
    apple: "/favicon-apple-touch-180.png",
    other: { rel: "icon", url: "/favicon-android-192.png" },
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
